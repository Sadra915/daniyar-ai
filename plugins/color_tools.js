function hex(h){h=h.replace('#','');if(h.length===3)h=h.split('').map(x=>x+x).join('');return [0,2,4].map(i=>parseInt(h.slice(i,i+2),16)/255)}function lum(c){return c.map(x=>x<=.03928?x/12.92:Math.pow((x+.055)/1.055,2.4))}module.exports={name:'color_tools',description:'محاسبه contrast رنگ‌های HEX برای UI accessible.',category:'طراحی',input_schema:{type:'object',properties:{foreground:{type:'string'},background:{type:'string'}},required:['foreground','background']},permission:'green',handler:async({foreground,background})=>{const a=lum(hex(foreground)),b=lum(hex(background));const la=.2126*a[0]+.7152*a[1]+.0722*a[2],lb=.2126*b[0]+.7152*b[1]+.0722*b[2];const ratio=(Math.max(la,lb)+.05)/(Math.min(la,lb)+.05);return JSON.stringify({ratio:Number(ratio.toFixed(2)),AA_normal:ratio>=4.5,AA_large:ratio>=3,AAA_normal:ratio>=7},null,2)}};