module.exports={name:'csv_tools',description:'تحلیل ساده CSV و تبدیل CSV به JSON.',category:'داده',input_schema:{type:'object',properties:{action:{type:'string',enum:['stats','to_json']},csv:{type:'string'},delimiter:{type:'string'}},required:['action','csv']},permission:'green',handler:async({action,csv,delimiter=','})=>{const rows=String(csv).trim().split(/\r?\n/).map(r=>r.split(delimiter));if(!rows.length)return 'CSV خالی است';const headers=rows[0];if(action==='stats')return JSON.stringify({rows:Math.max(0,rows.length-1),columns:headers.length,headers},null,2);return JSON.stringify(rows.slice(1).map(r=>Object.fromEntries(headers.map((h,i)=>[h,r[i]??'']))),null,2)}};