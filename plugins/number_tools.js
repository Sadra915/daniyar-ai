module.exports={name:'number_tools',description:'محاسبات عددی کوچک و دقیق برای میانگین، مجموع و آمار.',category:'داده',input_schema:{type:'object',properties:{action:{type:'string',enum:['sum','average','min','max','median']},values:{type:'array',items:{type:'number'}}},required:['action','values']},permission:'green',handler:async({action,values})=>{const a=values.map(Number).filter(Number.isFinite).sort((x,y)=>x-y);if(!a.length)return 'لیست عددی خالی است.';if(action==='sum')return String(a.reduce((x,y)=>x+y,0));if(action==='average')return String(a.reduce((x,y)=>x+y,0)/a.length);if(action==='min')return String(a[0]);if(action==='max')return String(a[a.length-1]);const m=Math.floor(a.length/2);return String(a.length%2?a[m]:(a[m-1]+a[m])/2)}};