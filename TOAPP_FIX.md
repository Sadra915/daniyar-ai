# ToApp / WebView connection fix

این نسخه برای حالتی آماده شده که رابط Daniyar AI با ToApp داخل WebView بسته‌بندی می‌شود.

## حالت 1 — Termux روی همان گوشی
1. در Termux سرور را اجرا کن:
   `npm install`
   `npm start`
2. در اپ ToApp وارد **تنظیمات → اتصال Backend** شو.
3. آدرس را بگذار:
   `http://127.0.0.1:3000`
4. روی «ذخیره و تست» بزن.

## حالت 2 — Backend روی دستگاه دیگری در شبکه
آدرس را با IP همان دستگاه عوض کن، مثلاً:
`http://192.168.1.10:3000`

## نکته Android / ToApp
اگر ToApp درخواست‌های `http://` را مسدود کند، باید در تنظیمات پروژه ToApp گزینه‌ای مانند **Cleartext Traffic / Allow HTTP** را فعال کنی، یا Backend را پشت HTTPS قرار بدهی.

## چه چیزی اصلاح شده؟
- تمام درخواست‌های API از یک API Base قابل تنظیم عبور می‌کنند.
- در حالت `file://` به‌صورت پیش‌فرض `http://127.0.0.1:3000` انتخاب می‌شود.
- CORS برای WebView/local-file origin در Backend اضافه شده.
- سرور روی `0.0.0.0` گوش می‌دهد تا اتصال LAN هم ممکن باشد.
- صفحه قبل از تست شبکه render می‌شود و `Failed to fetch` کل UI را متوقف نمی‌کند.
- دکمه «تست اتصال» خطای واقعی Backend را نشان می‌دهد.
